================================================================================
你最新遇到的错误：
================================================================================

Inconsistency in user input:
Force field 'oplsaa' occurs in 2 places, but not in the current directory.

Run without the -ff switch and select the force field interactively.

================================================================================
原因
================================================================================

GUI 代码里一直硬传 `-ff oplsaa`

但你的 GROMACS 安装里，oplsaa.ff 同时存在于多个地方（典型自定义安装问题），
导致 GROMACS 不知道该用哪个。

之前我已经修复了：
- 强制使用正确的 gmx.exe
- 强制设置 GMXDATA / GMXLIB

这次专门修复了**命令本身**（不传 -ff 参数）。

================================================================================
请立即同步
================================================================================

cd E:\1AI\GROMACS-GUI

git fetch origin
git pull origin arena/019f84dc-gromacs-gui

git log --oneline -3

# 确认看到这行：
# a1d4df4 fix: remove -ff parameter from pdb2gmx ...

然后**完全关闭当前 GUI**，重新启动：

.\gmx\Scripts\Activate.ps1
python src\main.py

================================================================================
重新测试步骤
================================================================================

1. 工作目录：E:\1AI\GROMACS-GUI\test
2. PDB：tai.pdb
3. 水模型：spce
4. 勾选 "忽略氢原子"
5. **不要管力场下拉框**（我们现在不传 -ff 了）
6. 点击 "运行 pdb2gmx"

现在应该能正常运行了。

如果还是失败，把新的完整日志贴出来。

================================================================================
