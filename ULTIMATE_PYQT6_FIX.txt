================================================================================
PyQt6 仍然 "DLL load failed" —— 其他真正有效的办法（直接复制执行）
================================================================================

你已经确认：
- gmx 完全正常
- VC++ 已安装
- 重装 PyQt6 6.11 无效

下面是**真正有效的其他办法**，请按顺序尝试。

--------------------------------------------------------------------------------
【办法 1 - 最推荐，先执行这个】降级到 PyQt6 6.6.1
--------------------------------------------------------------------------------

在你**当前这个 (gmx)** 窗口直接粘贴下面整段：

.\gmx\Scripts\Activate.ps1

pip uninstall PyQt6 PyQt6-Qt6 PyQt6-sip -y

pip install "PyQt6==6.6.1" "PyQt6-Qt6==6.6.1"

python -c "from PyQt6.QtCore import QCoreApplication; print('✅ PyQt6 6.6.1 加载成功！')"

如果上面出现 ✅ ，立刻启动 GUI：
python src\main.py

--------------------------------------------------------------------------------
【办法 2 - 最干净、最有效】完全重建虚拟环境（推荐如果办法1失败）
--------------------------------------------------------------------------------

# 退出当前环境
deactivate

# 删除旧的 gmx 文件夹
rmdir /s /q gmx

# 用**系统 Python**重新创建（非常重要！不要用 Spyder 的 python）
python -m venv gmx

# 激活新环境
.\gmx\Scripts\Activate.ps1

# 升级 pip
python -m pip install --upgrade pip

# 安装稳定版
pip install "PyQt6==6.6.1" "PyQt6-Qt6==6.6.1" matplotlib numpy

# 测试
python -c "from PyQt6.QtCore import QCoreApplication; print('✅ 新环境 PyQt6 加载成功！')"

# 启动
python src\main.py

--------------------------------------------------------------------------------
【办法 3 - 最简单】直接双击我准备好的文件
--------------------------------------------------------------------------------

在文件资源管理器里直接双击下面任意一个：

- `recreate_clean_venv.bat`   （推荐！自动完成重建 + 安装 6.6.1）
- `downgrade_pyqt6.bat`       （只降级当前环境）

双击后等它自动跑完，再尝试：
python src\main.py
或直接双击 start_gmx.bat

--------------------------------------------------------------------------------
请现在先执行【办法 1】（降级命令），把完整输出贴给我。
如果还是失败，直接双击 `recreate_clean_venv.bat`（这是目前最有效的办法）。
================================================================================
